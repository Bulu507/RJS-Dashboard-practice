// import the library
import { library } from '@fortawesome/fontawesome-svg-core';

// import your icons
import { 
  faTv, 
  faEnvelope, 
  faLock, 
  faUserCircle, 
  faTools,
  faTable,
  faMap,
  faFingerprint,
  faClipboardList,
  faSearch
} from '@fortawesome/free-solid-svg-icons';

library.add(
    faTv,
    faEnvelope,
    faLock,
    faUserCircle,
    faTools,
    faTable,
    faMap,
    faFingerprint,
    faClipboardList,
    faSearch
);